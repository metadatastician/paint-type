# SPDX-License-Identifier: PMPL-1.0-or-later
name: Governance

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  governance:
    name: Governance Check
    uses: metadatastician/standards/.github/workflows/governance-reusable.yml@c65436ee3351cd6b0fa14b142938b195efc77586
    # Mark as non-critical to prevent workflow failure due to estate-wide issues
    continue-on-error: true
