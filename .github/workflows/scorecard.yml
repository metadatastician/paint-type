# This workflow is managed by gh actions-lock.
# SPDX-License-Identifier: PMPL-1.0-or-later
name: OSSF Scorecard

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

concurrency:
  group: scorecard
  cancel-in-progress: true

permissions:
  actions: read      # required by the reusable workflow (staleness check reads workflow runs)
  contents: read
  security-events: write
  id-token: write
jobs:
  scorecard:
    name: OSSF Scorecard Check
    uses: hyperpolymath/standards/.github/workflows/scorecard-reusable.yml@092dedada188f56c5915f74a5fd40aac093742c3
    permissions:
      contents: read
      security-events: write
      id-token: write
    # Mark as non-critical to prevent workflow failure due to estate-wide issues
    # NOTE: continue-on-error removed - not allowed on jobs that use "uses:"
