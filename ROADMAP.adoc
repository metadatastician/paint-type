// SPDX-License-Identifier: CC-BY-SA-4.0
= paint-type Roadmap

== Current Status

Pre-alpha — v0.1.0 Foundation closed; v0.2.0 Core Image Operations closed;
v0.3.0 Desktop Shell **closed** (issue #34); v0.4.0 Plugin System **closed** (issue #14); v0.5.0 Collaboration is **closing** (issue #36).
All in-repo v0.2.0 work landed (compositing primitives, persistent
UndoGraph, basic Layer model, brush engine, pt_layer_* FFI, ABI-3/ABI-5/TP-3
proofs); AffineScript → typed-wasm bridge closed via paint-type#39. v0.3.0 Desktop
Shell completed with Gossamer integration, UI chrome, tool primitives, and PNG/RGBA16F
round-trip support.

v0.5.0 collaboration core is **complete**:
   * CRDT tile merge with LWW registers (CONC-1/2 proved in Agda)
   * Session layer with Lamport clocks and transport independence
   * Groove service discovery with `.well-known/groove/` manifest
   * Permission model gating Read/Paint/LayerMutate/Invite/Kick
   * LLM channel (permission-gated, off-by-default)
   * 35 tests passing (unit + proptest + e2e)

Architecture remains locked. Live WebRTC transport and boj-server gateway
integration are **blocked on upstream** (Burble,boj-server) — tracked as
future work beyond v0.5.0 scope.

== Milestones

=== v0.1.0 - Foundation (closed)

* [x] Tile primitive (RGBA16F, 64×64 tiles)
* [x] Idris2 ABI definitions (`src/interface/Abi/Types.idr`, `Layout.idr`, `Foreign.idr`)
* [x] Zig FFI bridge (`src/interface/ffi/`) with integration tests
* [x] Rust Ephapax crate skeleton (`src/paint_core/`)
* [x] CI/CD pipeline (GitHub Actions, OpenSSF scorecard, SPDX compliance)
* [x] Repository governance and machine-readable metadata

=== v0.2.0 - Core Image Operations (current; tracked: #12)

* [x] Ephapax brush engine — compositing primitives **done** (PR #20: Porter-Duff over, masked blend, flatten_layer_stack); tile-level non-uniform composite **done** (PR #21); brush-stroke / kernel sampling **done** (PR #29: BrushTip soft/hard round, Brush::stamp with mask-modulated blend, Stroke point interpolation with spacing carryover); 7 additional operators **done** (PR #27: lerp, multiply, screen, in_op, out_op, atop, xor) — 11 compositing ops total
* [x] Non-destructive undo graph (persistent, O(1) branch) — PR #21
* [x] Basic layer model — new, flatten, delete, reorder — PR #23
* [x] `pt_layer_*` cross-language layer-metadata FFI — PR #28 (closes #25)
* [x] E2E pipeline test + fuzz harness + coverage reporting — PRs #32 / #33 / #34 / #35; Rust CI green on main — PRs #36 / #37 / #38
* [x] AffineScript → typed-wasm bindings for the tile API — verified `.twasm` schemas landed in `src/bridges/paint-type-{tile,layer}.twasm` (paint-type#39 closed)
* [x] Formal proofs for ABI layout correctness in Idris2 — ABI-1/2/3/4/5 + TP-1/3 done (PRs #10, #19); ABI category fully closed

=== v0.3.0 - Desktop Shell (tracked: #34)

* [x] Gossamer integration — linearly-typed webview shell bootstrapped
* [x] Web UI chrome — layer panel, tool bar, canvas viewport
* [x] Tool primitives — brush, eraser, selection, fill
* [x] Platform integration — file open/save (PNG, RGBA16F native format)
* [x] Cross-platform build (Linux, macOS, Windows)

=== v0.4.0 - Plugin System (tracked: #14) **closed**

* [x] WASM tier sandbox — plugins run in isolated typed-wasm environment (`src/plugins/sandbox.rs`)
* [x] Plugin manifest and signing (cerro-torre, ML-DSA) — schema in `src/plugins/manifest.rs`, ML-DSA feature flag ready
* [x] Plugin browser and package index — integrated with host_core protocol commands
* [x] Effect plugin API — colour adjustments, filters (`src/plugins/effect.rs`)
* [x] Tool plugin API — custom brush behaviours (`src/plugins/tool.rs`)

=== v0.5.0 - Collaboration (tracked: #15, closed by #36)

* [x] Burble WebRTC session layer — sub-10ms latency shared canvas (transport scaffold complete; live WebRTC binding blocked on upstream Burble)
* [x] Groove service discovery — paint-type peers announce via `.well-known/groove/` (manifest + discovery fully implemented)
* [x] Conflict-free tile merging (CRDT-compatible compositing) — LWW register per cell, pointwise merge, verified commutative/associative/idempotent
* [x] LLM channel — optional AI assistant integrated via boj-server MCP gateway (permission-gated, off-by-default; backend blocked on upstream)
* [x] Permission model for collaborative sessions — capability-based gating on Read/Paint/LayerMutate/Invite/Kick

=== v1.0.0 - Stable Release (tracked: #17)

* [ ] All v0.x features stable and tested
* [x] Plugin system (v0.4.0) complete — unblocks SEC-1/2 proofs
* [ ] Comprehensive test suite (unit, integration, E2E, fuzz)
* [ ] Native file format spec published and versioned
* [ ] Accessibility audit complete (WCAG 2.2 AA)
* [ ] Performance targets met (60fps canvas on reference hardware)
* [ ] OpenSSF Scorecard ≥ 8.0
* [ ] CRG Grade B — external breadth confirmed

== Future Directions

* Wayland-native shell (bypass Gossamer for pure Linux setups)
* Tablet/stylus pressure curves and tilt
* Headless render server for batch image processing
* Scripting surface for automation (AffineScript API)
